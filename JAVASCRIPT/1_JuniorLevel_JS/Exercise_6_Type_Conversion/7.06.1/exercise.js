// # Exercise 6

// * Convert the `hello` variable to a boolean data type.
// * Convert the `age` variable to a string data type.
// * Convert the `isGraduated` variable to a numeric data type

let hello = 'Ciao';     // boolean
let age = 18;       // string
let isGraduated = false;     // number

let hello2 = true
let age2 = "18"
let isGraduated2 = 2

console.log(
    hello, 
    age,
    isGraduated,
    )

console.log(
    hello2, 
    age2,
    isGraduated2,
    )

//     # Exercise 6

// * Convert the `hello` variable to a boolean data type.
// * Convert the `age` variable to a string data type.
// * Convert the `isGraduated` variable to a numeric data type

let hello3 = 'ciao';     // boolean -> true o false
let age3 = "21";       // string -> comillas
let isGraduated3 = 0965;     // number -> 29038

Boolean(hello3);
String(age3);
Number(isGraduated3);

console.log(hello3, age3, isGraduated3)
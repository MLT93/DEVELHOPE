/* Exercise 1: Create a sum script

Create a script that outputs the total of 5 + 6 when you run it with Node.js. */

globalThis.console.log(`Hello there, ${process.env.USER}!`);

const sum = (a, b) => a + b;

console.log(sum(5, 6));
